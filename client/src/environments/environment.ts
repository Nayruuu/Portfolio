/**
 * Build-time environment config (development). `angular.json` `fileReplacements` swaps this file
 * for `environment.prod.ts` in production builds — so the right values are selected automatically.
 */
export const environment = {
  production: false,
  apiBaseUrl: '/api',
};
