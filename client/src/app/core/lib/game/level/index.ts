// core/lib/game/level — the LEVEL contract: the pure geometry-plus-placements shape every authored level
// (feature-side data) satisfies. Types only; no runtime. Re-exported through the game sub-barrel.
export * from './level';
