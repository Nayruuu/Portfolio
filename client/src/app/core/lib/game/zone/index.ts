// core/lib/game/zone — the OPEN-BUILDING world-state persistence: the per-zone snapshot type and the
// module-scoped store that remembers a zone while the player is elsewhere. Re-exported through the game
// sub-barrel (core/lib/game/index.ts).
export * from './zone-state';
