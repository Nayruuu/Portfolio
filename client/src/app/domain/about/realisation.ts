export interface Realisation {
  /** The project — what it is (leads the card). */
  title: string;
  /** A plain, factual line: what was done + the outcome, metric folded into the sentence (not hyped). */
  body: string;
}
