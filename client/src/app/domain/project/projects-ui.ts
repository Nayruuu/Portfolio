/** UI micro-copy for the `/projects` list + `/projects/:slug` detail pages. */
export interface ProjectsUi {
  title: string;
  subtitle: string;
  countLabel: string;
  open: string;
  overline: string;
  back: string;
  linksLabel: string;
  repo: string;
  nuget: string;
  docs: string;
  live: string;
  readArticle: string;
  stackLabel: string;
}
